# نشر مدار ونقله من Replit

## الخيار الأنسب

استخدمي استضافة PHP تدعم MySQL وSSL ونسخًا احتياطية يومية. يمكن أن يبقى الكود في GitHub، لكن تشغيل الحسابات والاختبارات سيكون على الاستضافة؛ GitHub Pages وحده لا يشغّل قاعدة البيانات أو PHP.

## النشر على استضافة مشتركة (cPanel مثلًا)

1. أنشئي قاعدة MySQL ومستخدمًا لها، وامنحيه صلاحيات القاعدة فقط.
2. من phpMyAdmin استوردي `database/schema.sql`.
3. ارفعي ملفات المشروع إلى مجلد الموقع.
4. أنشئي `.env` من `.env.example` وأدخلي بيانات القاعدة. لا ترفعي `.env` إلى GitHub.
5. اجعلي إصدار PHP 8.1 أو أحدث وفعّلي `pdo_mysql` و`mbstring` و`zip` و`fileinfo`، و`curl` عند استخدام الذكاء الاصطناعي.
6. عند تحديث موقع قائم، استوردي `database/migration_20260716_teacher_tools.sql` ثم `database/migration_20260717_madar_points.sql` ثم `database/migration_20260717_student_portfolio.sql` مرة واحدة لكل ملف.
7. من Terminal في لوحة الاستضافة شغّلي `php scripts/create_owner.php` مرة واحدة.
8. افتحي `/api/health`. يجب أن يظهر `"ok": true`.
9. افتحي `/owner/login.html` وسجلي الدخول، ثم احذفي كلمة المرور الأولية من `.env`.
10. فعّلي HTTPS قبل إنشاء حسابات حقيقية.

اجعلي المجلد `storage/private/student-portfolios` قابلًا للكتابة من PHP، ولا تزيلي قواعد منع الوصول المباشر إليه. أضيفي هذا المجلد إلى النسخ الاحتياطية مع قاعدة البيانات حتى لا تضيع ملفات إنجاز الطالبات.

ملف `.htaccess` يوجّه طلبات API ويمنع فتح ملفات الإعدادات وقاعدة البيانات مباشرة.

## Nginx

استخدمي `deploy/nginx.conf.example` كنقطة بداية، وعدّلي اسم النطاق ومسار المشروع ونسخة PHP-FPM. لا تستخدميه حرفيًا قبل تعديل القيم المشار إليها.

## النسخ الاحتياطي والنقل

للنقل إلى شركة استضافة أخرى تحتاجين شيئين فقط:

1. نسخة من ملفات GitHub، مع ملف `.env` محفوظًا بشكل آمن خارج GitHub.
2. تصدير قاعدة MySQL بصيغة SQL ونسخة من مجلد `storage/private/student-portfolios`.

بعد الاستيراد في الاستضافة الجديدة عدّلي بيانات `DB_*` في `.env`. لا توجد حاجة إلى Replit أو خدمته السحابية.

## قائمة فحص قبل استقبال الطالبات

- غيّري أي كلمة مرور ظهرت في لقطة شاشة أو محادثة.
- استخدمي كلمات مرور مختلفة للمالكة وقاعدة البيانات ومفتاح الذكاء الاصطناعي.
- جرّبي: إنشاء معلمة، موافقتها، إنشاء فصل وطالبة، تعيين كلمة مرور مؤقتة، تغييرها من حساب الطالبة، إنشاء اختبار ونشره وحله.
- راجعي الأسئلة المولّدة قبل اعتمادها.
- اختبري استعادة نسخة احتياطية، لا مجرد إنشائها.
- ضعي سياسة خصوصية مناسبة قبل إدخال بيانات طالبات حقيقية، واجمعي أقل قدر ممكن من البيانات.
