# تحديث الأمان وسلامة النتائج — 9 أغسطس 2026

هذا التحديث يمنع تزوير نتائج الألعاب، يحترم خيار إخفاء نتيجة الاختبار، يحمي النسخ الاحتياطية، ويكمل مخطط قاعدة البيانات.

## قبل رفع الملفات

1. خذي نسخة احتياطية من قاعدة البيانات الحالية باستخدام `mysqldump --single-transaction`.
2. لا ترفعي `.env` أو مجلد `.git` أو ملفات `storage/private` ضمن ملف مشاركة عام.
3. غيّري كلمة مرور قاعدة البيانات وكلمة مرور المالكة إذا سبق إرسال نسخة قديمة من المشروع.

## تحديث قاعدة موجودة

استوردي بالترتيب:

1. `database/migration_20260721_platform_enhancements_v11.sql`
2. `database/migration_20260808_teacher_interactive_games.sql`
3. `database/migration_20260808_interactive_game_audience.sql`
4. `database/migration_20260808_interactive_game_publication.sql`
5. `database/migration_20260809_security_integrity.sql`

ترحيل النشر يعطّل الألعاب القديمة عند إضافة نطاق الجمهور لأول مرة. أعيدي نشر كل لعبة من لوحة المعلمة واختاري الفصل أو النطاق بوضوح.

## إعداد `.env`

أضيفي:

```dotenv
ALLOW_RUNTIME_SCHEMA_CHANGES=false
BACKUP_DIR=/مسار/خاص/خارج/مجلد/الموقع
```

يجب أن يكون `BACKUP_DIR` قابلًا للكتابة من PHP وغير قابل للوصول من المتصفح. إذا تُرك فارغًا تستخدم المنصة مجلد `../../madar-backups`.

## التحقق بعد التحديث

1. افتحي `/api/health` وتأكدي أن `ok` تساوي `true`.
2. أنشئي جولة لعبة بحساب طالبة، وأجيبي عن الأسئلة، ثم تأكدي من حفظ النتيجة والشهادة.
3. أنشئي اختبارًا مع تعطيل «إظهار النتيجة»، وسلّميه بحساب طالبة، وتحققي أن الدرجة لا تظهر للطالبة أو ولي الأمر.
4. أنشئي نسخة احتياطية وتحققي أن رابط `/backups/اسم-الملف.sql` يعيد 404.
5. اختبري الاستعادة أولًا باستخدام `--dry-run`.
